spring:
  application:
    name: insurance-middle
  profiles:
    active: local
  config:
    import: "optional:configserver:http://insurance-configserver:8888"
management:
  endpoints:
    web:
      exposure:
        include: health,info,env,refresh
